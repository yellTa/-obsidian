---
created: 2024-07-21T18:58
updated: 2024-08-13T12:43
---
# 유지보수

```dataviewjs
const pages = dv.pages('"1_Projects/Burgerput/2_유지보수"')
  .where(p => p.file.name !== "2_유지보수");

dv.table(
  ["파일 이름", "생성 날짜", "수정 날짜", "Progress"],
  pages.map(p => {
    const progress = p.progress ? String(p.progress) : "";
    let fileName;

    if (progress.toLowerCase() === "unsolved") {
      fileName = `<a href="${p.file.path}" style="display: block; background-color: #ffcccc; padding: 5px; border-radius: 4px; text-decoration: none;">${p.file.name}</a>`;
    } else if (progress.toLowerCase() === "ongoing") {
      fileName = `<a href="${p.file.path}" style="display: block; color : #D1A243; padding: 5px; border-radius: 4px; text-decoration: none;">${p.file.name}</a>`;
    } else {
      fileName = `<a href="${p.file.path}" style="display: block; padding: 5px; border-radius: 4px; text-decoration: none;">${p.file.name}</a>`;
    }

    return [
      fileName,
      new Date(p.file.ctime).toISOString().split('T')[0],
      new Date(p.file.mtime).toISOString().split('T')[0],
      progress
    ];
  })
);

```
 