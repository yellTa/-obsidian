---
created: 2024-07-21T18:58
updated: 2024-08-13T12:17
---
# 유지보수
 

```dataviewjs
const pages = dv.pages('"1_Projects/Burgerput/2_유지보수"') .where(p => p.file.name !== "2_유지보수"); dv.table( ["파일 이름", "생성 날짜", "수정 날짜", "Progress"], pages.map(p => { const progress = p.progress ? String(p.progress) : ""; const formattedProgress = progress.toLowerCase() === "ongoing" ? `<span style="background-color: yellow; padding: 2px 4px;">${progress}</span>` : progress; return [ dv.fileLink(p.file.path), new Date(p.file.ctime).toISOString().split('T')[0], new Date(p.file.mtime).toISOString().split('T')[0], formattedProgress ]; }) );
```
 